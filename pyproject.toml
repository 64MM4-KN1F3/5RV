[project]
name = "bom-radar-scraper"
version = "0.1.0"
description = "A script to scrape and process BOM radar images."
requires-python = ">=3.8"
dependencies = [
    "requests",
    "Pillow",
    "pytz",
    "pre-commit>=3.5.0",
]

[tool.uv]
dev-dependencies = []
